const { Router } = require('express');
const { getFeatures } = require('../middlewares/ReviewMidd')
const router = Router();


router.get('/', async (req, res) => {
   
   
})

module.exports = router;
